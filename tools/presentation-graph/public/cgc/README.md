# Dossier `public/cgc/`

Ce répertoire est servi tel quel par Vite sous l’URL `/cgc/`.

- **`viz.html`** : fichier affiché dans l’iframe de la vue **Technique**. Par défaut, c’est une page placeholder. Remplacez-la par l’export HTML produit par [CodeGraphContext](https://github.com/CodeGraphContext/CodeGraphContext) (`--viz` sur une commande `analyze` / `find` selon la doc CLI).

Après remplacement, rechargez la page « Technique » (`/tech`) sur le serveur de dev.
